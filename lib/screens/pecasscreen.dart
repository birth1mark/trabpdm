import 'package:flutter/material.dart';
import '../res/theme.dart';

class PecasScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(backgroundColor: Color.Blue,),
    );
  }
}
